import React from "react";


const Photo = ({ selectedImageId }) => {
  return (
    <div>
        <h1>component is running</h1>
      <h2>Selected Image ID: {selectedImageId}</h2>
      {/* Other image component content goes here */}
    </div>
  );
};
export default Photo;